# Không chạy install script của dependency (xem docs/DEVELOPER_GUIDE.md §1.4).
# better-sqlite3 13 đã kèm sẵn binary (prebuilds/*.node) nhưng npm bỏ qua "gypfile": false
# và vẫn gọi `node-gyp rebuild` → cần Python + Visual C++ Build Tools, lỗi trên Windows.
ignore-scripts=true
